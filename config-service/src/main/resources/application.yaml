server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/edhydev/java-microservicios
          search-paths: config-data
  application:
    name: server-config
